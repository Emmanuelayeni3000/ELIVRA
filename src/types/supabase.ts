// This is a placeholder for Supabase types.
// You should generate your actual types using the Supabase CLI:
// `supabase gen types typescript --local > src/types/supabase.ts`
// after setting up your Supabase project and pushing your schema.

export type Database = {
  public: {
    Tables: {
      [_ in never]: never
    }
    Views: {
      [_ in never]: never
    }
    Functions: {
      [_ in never]: never
    }
    Enums: {
      [_ in never]: never
    }
    CompositeTypes: {
      [_ in never]: never
    }
  }
}